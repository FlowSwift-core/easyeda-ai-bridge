{
  "name": "easy-eda-bridge-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "Monorepo for EasyEDA Pro AI Bridge and Extension",
  "scripts": {
    "dev:bridge": "pnpm --filter bridge run server",
    "build:extension": "pnpm --filter extension run build"
  }
}
